<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.FFLOL.freefirenickname">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic__round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FreeFireNickname">
        <activity android:name=".Splashscreen">


        </activity>
        <activity android:name=".Theme" />
        <activity android:name=".AboutApp" />
        <activity android:name=".Help" />
        <activity android:name=".AditionalActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>